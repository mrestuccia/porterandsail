import React from 'react';
import { render } from 'react-dom';

const root = document.getElementById('root');

const App = () => {
  return (
    <div>
      hello world
    </div>
  );
};

render(<App />, root);
